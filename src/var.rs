fn main() {
    let x: i32 = 5; // i32 เป็นประเภทข้อมูลที่ใช้สำหรับจำนวนเต็ม 32-bit
    let y = 10; // Rust สามารถอนุมานประเภทข้อมูลได้
    let sum = x + y;
    println!("The sum of {} and {} is {}", x, y, sum);
}
